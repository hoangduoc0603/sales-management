# Release Hardening Readiness

**Trạng thái:** Blocked — chưa đủ điều kiện release bán được  
**Nguồn:** SRS overview, LLD readiness review, runtime/performance, deployment lifecycle, master implementation plan và Phase 12 plan.

## 1. Mục tiêu

Tài liệu này là artifact đọc độc lập cho Phase 12. Nó ghi rõ release hiện đang bị chặn bởi gap nào, cần bằng chứng gì để gỡ chặn và gap nào phải xin quyết định scope trước khi code tiếp.

## 2. P0 gaps — chặn release

| ID | Gap | Nguồn | Evidence cần có để đóng |
| --- | --- | --- | --- |
| `production-persistence-adapters` | Production persistence đã có adapter/repository/composition seam trong repo, nhưng chưa có Apps Script test-project drill chứng minh đọc/ghi trên tài nguyên Google thật. | `SRS-OVR-003`, `SRS-OVR-008`, `SRS-OVR-023`, `sheet-schema-and-registry.md` | SheetGateway/TableLocator/DriveGateway/RuntimeConfigStore có contract test với fake Apps Script globals; không hard-code ID/header/row; ít nhất sellable slice đọc/ghi qua adapter thật; drill trên Apps Script test project. |
| `pos-acceptance-benchmark` | Thiếu acceptance và benchmark cho POS sellable baseline. | `SRS-OVR-004`, `SRS-OVR-013`, `SRS-OVR-024`, `runtime-and-performance.md` | Cross-domain acceptance flow pass; POS scan/search/cart benchmark đạt p95; checkout local baseline có p95/p99; production Apps Script benchmark được ghi lại trước release thật. |
| `backup-restore-drill` | Thiếu backup/restore replacement-resource drill. | `SRS-OVR-010`, ADR 0007 | Backup manifest gồm partitions/resources/checksum; retention 30 daily; restore prepare tạo resource thay thế, verify, Owner switch, revoke session và health-check. |
| `scheduled-worker-runtime` | Scheduled worker runtime đã có baseline trong repo, đã nối import/export/archive/health/backup local, nhưng chưa có Apps Script scheduled trigger/dry-run evidence. Audit delivery đã bị loại khỏi baseline theo ADR 0017. | `SRS-OVR-021`, LLD Administration–Reporting–Operations §5 | BackgroundRun có lease/checkpoint/retry budget; worker không giữ ScriptLock trong việc nặng; scheduled trigger trên Apps Script test project; import/export/backup/archive có test/drill. |
| `deployment-migration-drill` | Thiếu deployment, migration và customer installation drill. | `SRS-OVR-023`, ADR 0006 | Deployment runbook; fresh tenant dry-run; upgraded tenant dry-run; backup trước migration; `.clasp.json` local-only. |

## 3. P1 gaps — cần chốt sellable scope

| ID | Gap | Nguồn | Hướng xử lý |
| --- | --- | --- | --- |
| `checkout-stale-conflict` | `PRICE_CHANGED` và `INSUFFICIENT_STOCK` đã được kiểm chứng ở Sales checkout và local fake backend; promotion/voucher/points conflict đang disabled theo baseline vì module tương ứng chưa bật. | Phase 4, `SRS-OVR-020` | `tests/apps-script/sales/sales-service.test.ts` và `tests/web/local-fake-backend.test.ts` cover stale quote không tạo order một phần. |
| `inventory-opening-lot-serial-transfer-stocktake` | Opening balance/import-safe, lot/serial guard, transfer/stocktake baseline và local concurrency/rounding matrix đã triển khai. UI/workbench baseline đã có; production benchmark vẫn xử lý trong POS/release hardening. | Phase 5, `SRS-INV` | `tests/apps-script/inventory/inventory-service-opening-lot-serial.test.ts`, `tests/apps-script/inventory/inventory-service-transfer-stocktake.test.ts`, `tests/apps-script/inventory/inventory-service-issue.test.ts` pass. |
| `finance-master-aging` | Đã triển khai backend/local fake cho CashDrawer/PaymentMethod master, `finance.master.get` và `finance.aging.get`; còn cần UI nghiệp vụ đầy đủ và benchmark/hardening theo màn Finance. | Phase 6, `SRS-FIN` | Backend slice pass với service/composition/shared/local fake tests; tiếp tục xử lý UI và performance/concurrency matrix trong các slice Finance sau. |
| `sales-attachment-policy-reversal` | Attachment Drive flow và CRM policy reversal còn mở. Deposit cancellation đã có backend/local fake coverage. | Phase 8, `SRS-SAL` | Cần quyết định có bật file đính kèm và CRM policy reversal đầy đủ ở release đầu không. |
| `reporting-drilldown-archive-worker-export` | Worker-backed export, drill-down permission revalidation và archive coverage đã có baseline local; production export/archive drill còn mở. | Phase 10, `SRS-ACC` | Export worker production drill và archive route drill trên Apps Script test project cần trước production nếu UI hiển thị export/drill-down. |
| `operations-production-lifecycle` | Import/backup/archive đã có local worker baseline; attachment private Drive lifecycle đã có production adapter. Backup/restore replacement resource và production lifecycle drill còn mở. Audit lifecycle đã bị loại khỏi baseline theo ADR 0017. | Phase 11, `SRS-OVR-009..011` | Chia lát cắt production hardening; backup/restore lifecycle là phần trọng yếu. |

## 4. Evidence đã có trong Phase 12

| Ngày | Gate | Evidence | Trạng thái |
| --- | --- | --- | --- |
| 2026-07-27 | Release readiness gate | `scripts/release-readiness.mjs`, `tests/release/release-readiness.test.ts`, `npm run release:readiness` trả `Blocked` với P0/P1 gaps. | Hoàn thành gate, release vẫn blocked. |
| 2026-07-27 | Sellable scope classification | `docs/architecture/release-scope-baseline.md`, `tests/release/sellable-scope.test.ts`. | Hoàn thành classification draft, cần user duyệt decision items. |
| 2026-07-27 | Production adapter seam | `SheetGateway`, `DriveGateway`, `RuntimeConfigStore`, `AppsScriptLockProvider` và `tests/apps-script/platform/google-workspace-adapter.test.ts`. | Adapter seam pass contract tests; P0 `production-persistence-adapters` vẫn mở cho tới khi sellable repositories cutover và chạy drill với tài nguyên thật. |
| 2026-07-27 | Append-only Sheet record repository seam | `apps-script/src/repositories/platform/sheet-record-repository.ts` và `tests/apps-script/platform/sheet-record-repository.test.ts` tạo primitive repository đọc/append typed record qua `SheetGateway`, route bằng `TableDefinitionDTO` + partition key, kiểm primary key bắt buộc và duplicate trước khi append. | Targeted repository test pass; đây là partial evidence cho P0 `production-persistence-adapters`. P0 vẫn mở vì domain sellable chưa cutover sang repository production và chưa có Apps Script test project drill. |
| 2026-08-01 | AuditOutbox repository superseded | Theo ADR 0017, `AuditOutbox`/`AuditLog` không còn là baseline. Repository/worker/table registry audit đã bị gỡ khỏi source hiện hành; truy vết dùng actor metadata trên record. | P0 hiệu năng POS giảm một write path trong checkout; nếu cần audit enterprise sau này phải thiết kế optional module bằng ADR mới. |
| 2026-07-27 | Sheet-backed CommandRepository journal | `createSheetCommandRepository` trong `apps-script/src/repositories/platform/command-repository.ts` và `tests/apps-script/platform/command-sheet-repository.test.ts` lưu mỗi `save()` thành một version append-only (`commandId:vN`), query trả bản ghi mới nhất theo `updatedAt`/version và bổ sung header `errorCode` cho `CommandTransaction`. | Targeted repository test pass; đây là partial cutover cho command journal không dùng row number/update cứng. P0 vẫn mở vì composition production chưa wiring và chưa có dry-run trên Apps Script test project. |
| 2026-07-27 | Production platform repository wiring seam | `apps-script/src/bootstrap/create-production-platform-repositories.ts` và `tests/apps-script/platform/production-platform-repositories.test.ts` tạo factory nhận `SheetGateway`, `TableDefinitionDTO[]` và active transaction partition để trả về `CommandRepository` sheet-backed; fail-fast nếu thiếu table definition. | Targeted wiring test pass; P0 vẫn mở vì factory chưa được nối vào production Apps Script composition đầy đủ, domain repository trọng yếu chưa cutover và chưa có deployment drill thật. |
| 2026-07-27 | Sheet-backed Catalog repository cho POS projection | `createSheetCatalogRepository` trong `apps-script/src/repositories/catalog/catalog-repository.ts` và `tests/apps-script/catalog/catalog-sheet-repository.test.ts` đọc/append versioned Product, Variant, VariantBarcode và UnitConversionVersion qua `SheetGateway`; registry bổ sung các cột POS-critical như `tenantId`, `sku`, `unitPriceVnd`, `barcode`, `unitName`, `schemaVersion`, `recordVersion`. | Targeted Catalog repository test pass; đây là domain sellable cutover đầu tiên cho POS catalog/projection. P0 vẫn mở vì Inventory/Finance/Sales/Operations repository, production composition wiring và Apps Script drill thật chưa hoàn tất. |
| 2026-07-27 | Sheet-backed Inventory repository cho movement/balance | `createSheetInventoryRepository` trong `apps-script/src/repositories/inventory/inventory-repository.ts` và `tests/apps-script/inventory/inventory-sheet-repository.test.ts` append `InventoryMovement` immutable qua active transaction partition, lưu `InventoryBalance` dạng versioned projection và đọc latest balance theo `warehouseId + variantId`; registry bổ sung base/scope/correlation columns cho movement/balance. | Targeted Inventory repository test pass; đây là domain sellable cutover thứ hai cho POS stock path. P0 vẫn mở vì Finance/Sales/Operations repository, production composition wiring và Apps Script drill thật chưa hoàn tất. |
| 2026-07-27 | Sheet-backed Finance repository cho payment/shift/cash path | `createSheetFinanceRepository` trong `apps-script/src/repositories/finance/finance-repository.ts` và `tests/apps-script/finance/finance-sheet-repository.test.ts` lưu master/versioned `CashDrawer`, `PaymentMethod`, `Shift`, `Payment`, `ReceivableLedger`, `PayableLedger`, `CustomerCredit`, `SupplierPrepayment`; append-only `CashTransaction` và `PaymentAllocation` đi active transaction partition; registry bổ sung base/correlation/source columns cho finance tables. | Targeted Finance repository test pass; đây là domain sellable cutover thứ ba cho POS payment/shift path. P0 vẫn mở vì Sales/Operations repository, production composition wiring và Apps Script drill thật chưa hoàn tất. |
| 2026-07-27 | Sheet-backed Sales repository cho POS/order/return/warranty path | `createSheetSalesRepository` trong `apps-script/src/repositories/sales/sales-repository.ts` và `tests/apps-script/sales/sales-sheet-repository.test.ts` lưu versioned `SaleOrder`, replacement-set `SaleOrderLine`/`SaleTenderDraft`, immutable `ReceiptSnapshot`, `SaleReturn` + `SaleReturnLine` và `WarrantyCase` qua active transaction partition; registry bổ sung đủ bảng Sales/POS/Return/Warranty và child-set marker cho set rỗng. | Targeted Sales repository + registry tests pass; đây là domain sellable cutover thứ tư cho POS order/receipt/return path. P0 vẫn mở vì Operations repository, production composition wiring và Apps Script drill thật chưa hoàn tất. |
| 2026-08-02 | Online deposit cancellation | `SalesService.cancelOnline` xử lý đơn online có đặt cọc trước `Shipped`: giữ tiền thành `CustomerCredit` hoặc tạo refund counter-payment; local fake backend cùng parser request đã đồng bộ. | `tests/apps-script/sales/sales-service.test.ts`, `tests/shared/sales-contracts.test.ts`, `tests/web/local-fake-backend.test.ts` pass targeted. |
| 2026-07-27 | Sheet-backed Operations repository cho import/attachment/worker/backup/restore telemetry | `createSheetOperationsRepository` trong `apps-script/src/repositories/operations/operations-repository.ts` và `tests/apps-script/operations/operations-sheet-repository.test.ts` lưu versioned `ImportBatch`, `AttachmentMetadata`, `BackgroundRun`, `RestoreRun`, `PartitionRegistry`, `HealthCheck`, `CapacityAlert`; replacement-set `ImportStagingRow`, `BackupRun` retention và `RuntimeRecord`; registry bổ sung bảng/headers Operations runtime/evidence. | Targeted Operations repository + registry tests pass; đây là domain cutover cho metadata vận hành. P0 `production-persistence-adapters` vẫn mở cho tới khi production composition wiring và Apps Script test-project drill chứng minh đọc/ghi trên tài nguyên thật. |
| 2026-08-02 | Private Drive lifecycle adapter cho attachment | Thêm `operations.attachment.upload/list/delete/download`, `AttachmentStorage` seam, `DriveGateway.save/read/trashPrivateAttachment`, production wiring qua thư mục `driveFolders.attachments`, local fake parity và tests `operations-contracts`, `operations-service`, `operations-composition`, `google-workspace-adapter`, `local-fake-backend`. | File đính kèm đã ghi/đọc/trash qua private Drive và chỉ lưu metadata, không trả public URL. Tích hợp UI/domain source object vẫn là follow-up ở từng domain. |
| 2026-07-27 | Sheet-backed CRM/Purchasing/Reporting repositories | `createSheetCustomerRepository`, `createSheetPurchasingRepository`, `createSheetReportingRepository` và các tests `customer-sheet-repository`, `purchasing-sheet-repository`, `reporting-sheet-repository` lưu customer lookup, supplier/PO/receipt/return/cost variance, dashboard projection/report rows/export runs qua `SheetGateway`; registry bổ sung bảng/headers tương ứng. | Targeted CRM/Purchasing/Reporting repository tests pass; các domain còn lại của sellable baseline đã có sheet-backed seam. P0 vẫn mở vì production composition/runtime config và Apps Script test-project drill chưa chạy trên tài nguyên thật. |
| 2026-07-27 | Production repository aggregate wiring seam | `apps-script/src/bootstrap/create-production-repositories.ts` và `tests/apps-script/platform/production-repositories.test.ts` gom `CommandRepository`, Catalog, CRM, Inventory, Purchasing, Finance, Sales, Reporting và Operations sheet-backed repositories vào một factory nhận `SheetGateway`, `TableDefinitionDTO[]` và active transaction partition; fail-fast khi thiếu bảng trọng yếu. | Targeted aggregate wiring test pass; P0 vẫn mở vì `createApiComposition` production/runtime config chưa cutover và chưa có Apps Script test-project drill trên tài nguyên thật. |
| 2026-07-27 | Production platform metadata persistence và API composition cutover seam | `createSheetAuthRepository`, `createSheetAdministrationRepository`, `createPropertiesCredentialVerifierStore`, `createHmacSha256PasswordService`, `createProductionApiComposition`, `createAppsScriptProductionComposition`, `createActiveRuntimeTableLocator`; tests `auth-sheet-repository`, `administration-sheet-repository`, `credential-verifier-store`, `password-service`, `production-api-composition`, `runtime-table-locator`. | Local targeted tests pass; metadata user/session/admin lưu qua Sheets, credential verifier tách khỏi Sheets sang Properties, `invoke_` Apps Script tạo production composition từ runtime config. P0 `production-persistence-adapters` vẫn mở cho tới khi chạy drill trên Apps Script test project thật. |
| 2026-07-27 | Cross-domain acceptance harness | `tests/apps-script/release/acceptance-flow.test.ts` chạy qua `createApiComposition().invoke(...)`: bootstrap/auth/scope/POS/order/dashboard, online/receivable/return refund, purchasing/payable/supplier payment và backup/restore/health. | Acceptance harness pass local in-memory; P0 `pos-acceptance-benchmark` vẫn mở cho tới khi có POS benchmark và Apps Script production evidence. |
| 2026-07-27 | POS performance local baseline | `tests/performance/pos-performance.test.ts` và `tests/performance/fixtures/pos-seed.ts` seed 10.000 variants, đo warm scan/search/cart trong browser-local cache và checkout qua local in-memory `createApiComposition()`. | Node benchmark pass các ngưỡng SRS p95/p99; chưa thay thế Apps Script production benchmark trước release thật. |
| 2026-07-27 | Security release review | `tests/apps-script/release/security-review.test.ts` kiểm reset/disable/access change revoke session bằng `authVersion`, không lộ token/password trong API/export, chặn bypass Warehouse scope và loại sensitive fields khỏi dashboard/report/export. | Security review local pass; P0 `security-release-review` có executable evidence cho session/scope/sensitive baseline. |
| 2026-07-27 | Worker/backup/restore/archive baseline | `apps-script/src/services/platform/worker/background-runner.ts`, `apps-script/src/services/operations/backup-restore-worker.ts`, `apps-script/src/services/operations/archive-worker.ts` và `tests/apps-script/release/worker-backup-restore.test.ts`. | Local worker baseline pass cho lease/checkpoint/retry, manifest/checksum/30 daily retention, replacement restore switch và archive read-only routing; production Drive/Sheets drill vẫn cần deployment evidence. |
| 2026-07-27 | Scheduled worker runtime và Apps Script entrypoint baseline | `apps-script/src/services/platform/worker/scheduled-worker-runtime.ts`, `apps-script/src/bootstrap/run-production-scheduled-worker.ts`, export `scheduledWorker_` trong `apps-script/src/bootstrap/entry.ts`; tests `scheduled-worker-runtime` và `production-scheduled-worker`. | Local scheduled tick pass cho `BackgroundRun` lease/checkpoint/retry, import commit chunk, worker-backed reporting export, archive closed partition, health check nhẹ và daily backup manifest. P0 `scheduled-worker-runtime` vẫn mở cho tới khi tạo scheduled trigger trên Apps Script test project và chạy dry-run production. |
| 2026-07-27 | Import commit worker | `apps-script/src/services/operations/import-commit-worker.ts`, `tests/apps-script/operations/import-commit-worker.test.ts`, `listImportBatches()` trong OperationsRepository và scheduled worker wiring. | Local chunking pass: batch `Committing` commit valid staging rows theo chunk, skip invalid rows khi hoàn tất và retry không tạo lại `sourceObjectId`. Production Apps Script trigger/drill vẫn pending. |
| 2026-07-27 | Archive scheduled worker baseline | `runArchiveChunk` trong `apps-script/src/services/operations/archive-worker.ts`, `tests/apps-script/operations/archive-worker.test.ts` và scheduled worker wiring `scheduled-archive-transaction`. | Local idempotency pass: closed transaction partition chuyển sang `Archived`, `readOnly=true`, giữ nguyên `archivedAt` khi retry; scheduled tick ghi BackgroundRun checkpoint. Production Apps Script trigger/drill vẫn pending. |
| 2026-07-27 | Worker-backed reporting export baseline | `apps-script/src/services/reporting/reporting-export-worker.ts`, `tests/apps-script/reporting/reporting-export-worker.test.ts` và scheduled tick wiring khi có `reportingRepository`. | Local worker hoàn tất `ReportingExportRun` routing `LargeWorker`, ghi `completedAt`, `rowCount`, `fileId` và idempotent khi chạy lại. Drive file render/export production thật và permission drill vẫn pending. |
| 2026-07-27 | Reporting drill-down permission baseline | `reporting.drillDown.resolve`, `ReportingDrillDownTokenDTO`, schema parser và tests `reporting-contracts`, `reporting-service`, `reporting-composition`. | Local resolver revalidate actor scope từ token, dùng quyền hiện tại để loại sensitive fields và đi qua single API gateway với `reporting.report.view`. Archive coverage và production drill vẫn pending. |
| 2026-07-27 | Reporting archive coverage baseline | `apps-script/src/services/reporting/reporting-partition-coverage.ts`, `tests/apps-script/reporting/reporting-partition-coverage.test.ts` và `ReportingReportQueryRequest.includeArchive`. | Local report query trả metadata `Partial` + `missingArchiveReason` khi date range cần archived transaction partition nhưng `includeArchive=false`; khi include archive rõ ràng thì coverage `Complete` và `archiveIncluded=true`. Production archive route drill vẫn pending. |
| 2026-07-27 | UI accessibility và Approved screen acceptance | `tests/web/release-ui-acceptance.test.ts` kiểm mọi handoff `Approved` có route/shell hoặc `CanShipDisabled`, custom Listbox có ARIA/keyboard source, POS scan input có `aria-label`/Enter/auto-focus, command button giữ label và có `aria-busy`, theme toggle là icon button. Bổ sung `web/src/features/reporting/reporting-administration-operations-home.tsx` cho artifact Reporting/Administration/Operations đã Approved. | Targeted UI acceptance pass. Open Design MCP trả `Transport closed` và Local Preview `127.0.0.1:61609` không truy cập được tại thời điểm kiểm tra, nên visual/manual comparison light/dark với artifact hiện được ghi pending, không suy diễn từ screenshot. |
| 2026-07-27 | Deployment runbook/customer installation checklist | `docs/architecture/deployment-runbook.md` mô tả prerequisite Google account khách, `.clasp.json` local-only, `npm run deploy:push`, versioned deployment, tenant bootstrap, admin handoff, health/readiness, backup trước upgrade, emergency restore và evidence cần lưu ngoài Git. `scripts/deploy-push.mjs` có guard chặn push khi thiếu `scriptId` hoặc `rootDir` khác `./dist`; `tests/release/deploy-push-safety.test.ts` giữ regression này. | Runbook baseline và safety guard pass targeted test; production deployment/migration/restore drill trên Apps Script test project vẫn là P0 evidence pending trước khi release `Ready`. |
| 2026-07-27 | Final release gate — local evidence | `npm run verify` pass với structure, typecheck, lint, 98 test files/260 tests, build và Apps Script artifact check. `npm test -- tests/performance/pos-performance.test.ts` pass. `npm run release:readiness` trả `Blocked` với P0 còn mở: production persistence Apps Script drill, POS Apps Script production evidence, backup/restore drill, scheduled worker trigger/drill và deployment/migration drill. `npm run deploy:push` chạy verify pass rồi dừng an toàn vì `.clasp.json` local không tồn tại. | Release vẫn `Blocked`; không tick deployment dry-run hoặc restore drill cho tới khi có Apps Script test project và evidence thật theo runbook. |
| 2026-08-01 | Apps Script `/dev` production acceptance drill — preflight và push test | [`2026-08-01-apps-script-production-acceptance-drill.md`](../superpowers/plans/2026-08-01-apps-script-production-acceptance-drill.md) tạo checklist drill. `npm run release:readiness` trả `Blocked` với 5 P0 gap hiện hành. Targeted tests `acceptance-flow`, `security-review`, `pos-performance`, `runtime-warmup-service`, `warmup-trigger-manager` pass 5 files/16 tests. `npm run build`, `npm run check:artifact` pass. `npm run deploy:test` pass full `npm run verify` với 108 test files/321 tests, build/artifact pass và push code mới nhất lên Apps Script `/dev` không tạo version mới. | Preflight và push `/dev` pass. Release vẫn `Blocked` vì chưa có evidence manual trên Web App cho login/dashboard/POS timing, health/backup, scheduled worker và restore/migration drill. |
| 2026-08-01 | Apps Script `/dev` production acceptance drill — browser/CLI blocker | Chrome mở `/dev` trả “Bạn cần có quyền truy cập” vì browser đang dùng Google account không có quyền editor/test deployment; agent không gửi request quyền. `npx clasp deployments --json` xác nhận có HEAD/test deployment và một versioned deployment. `npx clasp run getWarmupTriggerStatus` trả `Script function not found. Please make sure script is deployed as API executable.` | Browser drill chưa thể tiếp tục trong agent session hiện tại. Cần user mở `/dev` bằng đúng Google account owner/editor để thu console timing, hoặc tạo API executable nếu muốn kiểm tra function qua `clasp run`. |
| 2026-08-01 | Apps Script `/dev` production acceptance drill — Chrome profile connected và debug buffer | Sau khi user bật Chrome extension ở đúng profile, connector thấy profile `Kevin 1` và claim được tab `/dev?debugApi=1`. Bổ sung `window.__CENIO_API_DEBUG_LOGS__` chỉ khi debug API bật để lưu sanitized API timing cho drill; test TDD `tests/web/api-client.test.ts` pass. `npm run deploy:test` pass full verify 108 test files/322 tests và push lại `/dev`. Reload `/dev?debugApi=1` hiển thị màn đăng nhập nội bộ. | Browser drill đã qua blocker profile. Đang chờ user nhập mật khẩu admin trực tiếp trong tab để thu login/dashboard/POS timing; agent không nhập hoặc hỏi mật khẩu qua chat. |
| 2026-08-01 | Apps Script `/dev` production acceptance drill — runtime/debug hardening | Browser drill phát hiện 2 lỗi release-critical: HtmlService sandbox có thể không cho gắn debug state lên `window`, và frontend có thể fallback `local-fake` nếu `google.script.run` chưa sẵn sàng tại lúc React mount. Đã sửa bằng boot config JSON DOM-safe, debug path không throw khi `window` non-extensible, runtime detection ưu tiên Apps Script theo URL/boot và Apps Script invoker chờ bridge ngắn hạn trước khi trả transport error. Targeted tests `api-client`, `web-app-entry`, `runtime-client`, `google-script-run` pass; `npm run deploy:test` pass full verify 109 test files/327 tests và push lại `/dev`. | Runtime/debug hardening đã có local + `/dev` deploy evidence. Release vẫn `Blocked`: chưa thu được timing login/dashboard/POS thật sau fix vì Chrome-extension automation đọc được frame nhưng submit/click trong Apps Script sandbox không kích hoạt ổn định, Chrome DevTools connector không attach được page. |
| 2026-08-01 | Apps Script `/dev` production acceptance drill — production session ID fix | Chrome extension claim được tab `/dev?debugApi=1` và login bằng credential test app đã được user cấp. Debug DOM `#cenio-api-debug-logs` xác nhận `platform.auth.login` ok nhưng `reporting.dashboard.get` fail `SESSION_EXPIRED` vì production composition đang reset deterministic ID generator mỗi request (`session-1`, `sesrec-2`). Đã thêm injectable `idGenerator`, production Apps Script dùng `Utilities.getUuid()`, test regression `production-api-composition` cover unique session across composition instances. `npm run deploy:test` pass full verify 109 test files/328 tests và push lại `/dev`. | Login/session blocker đã xử lý. Sau fix, Dashboard không còn `SESSION_EXPIRED`; trạng thái còn lại là `DASHBOARD_NOT_READY` do chưa có `DashboardProjection` cho ngày/phạm vi hiện tại. Release vẫn `Blocked` cho tới khi có projection/worker evidence và các drill P0 còn lại. |
| 2026-08-01 | Apps Script `/dev` production acceptance drill — dashboard baseline projection, scheduled worker, health và backup evidence | TDD thêm baseline projection current-day cho tenant bootstrap và repair idempotent qua `platform.install.getStatus`; scheduled worker thêm job `ReportProjection` để tạo baseline ngày hiện tại nếu thiếu. `npm run deploy:test` pass full verify 110 test files/333 tests và push `/dev`. Google Workspace adapter production persistence drill pass: tenant đã được khởi tạo bằng Drive folders + Core/Runtime/Transaction/Audit Google Sheets thật, runtime config lấy từ Script Properties, repository production đọc/ghi qua `SheetGateway`, và browser drill xác nhận `platform.session.bootstrap`, `reporting.dashboard.get`, `catalog.pos.getProjection` chạy trên `/dev` thật. Browser drill `/dev?debugApi=1` xác nhận `reporting.dashboard.get` trả `ok=true` cho `2026-08-01`, 4 KPI = 0, empty trend/decision/manual-order. Timing sau marker: `platform.install.getStatus` server 137ms, `platform.session.bootstrap` server 739ms, `reporting.dashboard.get` server 1595ms. POS route render pass; `catalog.pos.getProjection` ok server 1902ms nhưng không có suggested product vì tenant test chưa có catalog seed. Apps Script Triggers page hiển thị `warmRuntime_` time-driven error rate 0% và `scheduledWorker_` time-driven. Function `scheduledWorker` chạy thủ công 11:49:24–11:49:42 và hoàn tất. Owner-managed function `runHealthCheck` chạy 12:02:27–12:02:30, log `operations.health.check` `ok=true`, `status=Ok`, `Integrity`, `capacityAlerts=[]`. Owner-managed function `requestManualBackup` chạy 12:02:47–12:02:51, log `operations.backup.request` `ok=true`, backup `Completed`, checksum manifest `checksum-cb4c0f85`. | Dashboard runtime blocker đã xử lý; scheduledWorker_ trigger Apps Script evidence đã có; health/manual backup smoke trên Apps Script thật đã có. Release vẫn `Blocked`: POS checkout production timing chưa có do thiếu catalog/tồn/ca POS test data; restore replacement-resource drill và deployment/migration drill còn mở. |
| 2026-08-01 | Apps Script `/dev` POS production acceptance và benchmark | TDD bổ sung owner-managed helper `runPosAcceptanceDrill`/`runPosAcceptanceDrill_` vào Apps Script artifact, seed fixture SKU `POS-ACCEPT-001`, tồn kho, payment method, cash drawer và shift `shift-local-open` tương thích POS UI. Fix production lock provider fallback từ `DocumentLock` sang `ScriptLock` cho standalone Apps Script Web App. `npm run deploy:test` pass full verify 111 test files/338 tests và push `/dev`. Apps Script editor chạy `runPosAcceptanceDrill`: functional checkout pass `Completed`/`Paid`, receipt `K80`, total `84.000đ`, `SaleIssue`, finance payment `Approved`. Stage timing cho run có breakdown: `projectionMs=956`, `quoteMs=1`, `checkoutMs=9786`, `totalMeasuredMs=15347`, `durationMs=15364`. Web App `/dev?debugApi=1` login/open POS/add fixture/checkout UI thành công với order `SO-SALE-ORDER-ACCE7431-DBC4-4710-A2E2-021E07B23277`; console ghi `sales.pos.complete pos-complete-1785574179922`, net BUSY→IDLE khoảng 14,6s. | Functional POS baseline trên Apps Script thật đã pass. P0 `pos-acceptance-benchmark` vẫn `Blocked` vì checkout production vượt SRS-OVR-013 p95 ≤ 3s/p99 ≤ 5s. Bottleneck nằm ở production checkout write path qua Google Sheets, cần tối ưu repository/gateway command write path trước khi release. |
| 2026-08-01 | Apps Script `/dev` POS production benchmark — broad write-path optimization | Tối ưu theo hướng toàn app: Catalog master CacheService với size cap/invalidation, POS checkout reuse projection một lần, Inventory targeted availability theo variant trong giỏ, InventoryBalance execution-local version cache, InventoryMovement service-generated append fast path, và SheetGateway production defer append + flush cuối request qua Google Sheets Advanced Service `spreadsheets.values.batchUpdate`. Manifest khai báo `dependencies.enabledAdvancedServices` cho Sheets v4. `npm run deploy:test` pass full verify 109 files/352 tests và push `/dev`. Apps Script editor chạy `runPosAcceptanceDrill` sau batch write: run cold-ish `checkoutMs=3208`, `totalMeasuredMs=8910`, `durationMs=9237`; warm run `checkoutMs=2942`, `totalMeasuredMs=8113`, `durationMs=8457`; `sheetAppendDeferredRows=13`, `sheetBatchAppendFlushCount=4`, `sheetBatchAppendFlushRows=13`. | Server-side POS checkout warm đã sát/đạt mục tiêu p95 3s trong helper (`2942ms`). Release vẫn chưa chuyển `Ready`: cần UI benchmark trực tiếp sau khi clear/refresh POS catalog cache, và cần tiếp tục giảm total request/seed/find overhead nếu p95 nhiều lần chưa ổn định dưới 3s. |
| 2026-08-01 | Apps Script `/dev` POS UI benchmark — cache invalidation và 5 mẫu checkout thật | Browser drill phát hiện POS vẫn hiển thị `catalog-pos-0` sau khi helper seed fixture vì `runPosAcceptanceDrillForAppsScript_` tạo repository production không truyền `platformCacheStore`, nên `CacheService` giữ danh sách Catalog rỗng cũ. Đã sửa helper để dùng cùng `createAppsScriptCacheStore`, bổ sung regression frontend cho cache projection rỗng phải refresh, cập nhật UI acceptance map cho toàn bộ handoff Approved hiện tại. `npm run deploy:test` pass full verify 109 files/353 tests và push `/dev`. Sau khi chạy helper lại, API `catalog.pos.getProjection` trả `catalog-pos-2`, UI POS hiển thị 2 gợi ý. 5 checkout UI thật trên `/dev?debugApi=1` đều `ok=true`; client ms `[10702, 7741, 6798, 10423, 8822]`, server ms `[8022, 5179, 5049, 7372, 6243]`, `command.totalWithLockMs` `[6570, 4053, 3635, 5792, 5195]`. Trung bình: client `8897ms`, server `6373ms`, command lock `5049ms`; bottleneck chính `sheet.findRowsByColumnMs` trung bình `3401ms` và `sheet.appendRowsMs` trung bình `2082ms`. | Functional POS UI trên Apps Script thật pass, nhưng P0 `pos-acceptance-benchmark` vẫn `Blocked`: checkout UI warm còn vượt p95 ≤ 3s/p99 ≤ 5s. Cần tối ưu tiếp theo ở command/session/read-model path: giảm `findRowsByColumn`, giảm số table read/find trong lock, và gom/giảm append/persist order-finance-inventory trước khi release. |
| 2026-08-01 | Apps Script auth/session hot-path cache | `SheetAuthRepository` cache session metadata theo token fingerprint trong `CacheService`, cập nhật cache khi login/session refresh/logout và vẫn fallback Sheets khi miss. Không cache raw token/password/verifier; mỗi request vẫn revalidate user `authVersion`, disabled state, idle/absolute expiry và revoked state. Regression `auth-sheet-repository` chứng minh request sau login không đọc/tìm `Session` sheet khi cache hit; targeted auth/session/security/POS repository tests pass 6 files/26 tests. | Tối ưu này giảm một `findRowsByColumn` phổ biến cho mọi API đã đăng nhập, không chỉ POS. P0 POS benchmark vẫn `Blocked` cho tới khi benchmark `/dev` xác nhận checkout UI đạt p95 ≤ 3s/p99 ≤ 5s và các bottleneck command/domain lookup còn lại được xử lý. |
| 2026-08-01 | Apps Script POS UI benchmark sau session cache | `npm run deploy:test` pass full verify và push `/dev`. Browser drill trên `/dev?debugApi=1` sau reload xác nhận `platform.session.bootstrap`/`reporting.dashboard.get`/`catalog.pos.getProjection` vẫn ok. Checkout POS UI mới tạo 3 mẫu sau session cache: client `7754ms`, `5559ms`, `6095ms`; server `5095ms`, `3634ms`, `3724ms`. Auth path giảm rõ: `authMs` còn `54–67ms`, `session.findSessionMs` còn `8–34ms`, `sheetOpenCount=1` và không còn đọc `Session` sheet trên cache hit. Bottleneck còn lại: `sheet.findRowsByColumnMs` `1494–2181ms`, `sheet.appendRowsMs` `1147–2039ms`, `sheetAppendRows=9`, `command.totalWithLockMs` `3059–4529ms`. | Session cache đạt mục tiêu tối ưu chung cho authenticated API path. POS checkout UI vẫn `Blocked` vì client còn >5s và server chưa ổn định dưới p95 ≤ 3s; bước tối ưu tiếp theo phải nhắm vào command/domain lookup và write-path Google Sheets, không phải auth. |
| 2026-08-01 | Apps Script command/write-path payload slimming, exact lookup cache và Shift cache | `SheetGateway` reuse append sheet state trong một execution để không đọc lại `lastRow` cho từng append cùng sheet, cache exact lookup theo table/partition/column/value trong một execution và invalidate khi append cùng table. Registry bổ sung unique `lookupKeys` cho các khóa nóng `CommandTransaction.commandId`, `CommandTransaction.idempotencyKey`, `InventoryBalance.balanceId` và `Shift.shiftId` để gateway dùng targeted lookup thay vì full scan bảng nhỏ. `SheetCommandRepository` cache command result snapshot theo idempotency key TTL 6 giờ; `CommandCoordinator` dùng cache-only lookup cho retry gần và không scan durable `CommandTransaction` trước command mới trên fast path. Frontend POS giữ `commandId`/`idempotencyKey` ổn định cho cart đang complete để retry gần có thể hit cache thay vì tạo command mới. Single-line POS bỏ stock precheck riêng vì `issueForSale` tự kiểm availability trước khi ghi movement/projection; multi-line vẫn precheck để tránh partial side effects. `SheetFinanceRepository` cache `Shift` theo `shiftId` TTL 5 phút qua `CacheService`, cập nhật cache khi save/open/close/lock shift và fallback Sheets khi miss. API boundary `sales.pos.complete` trả/lưu command result snapshot tối thiểu cho UI/retry gồm order, lines, receipt, conflicts và receivable nếu có; không đưa `InventoryMovement`, `FinancePaymentRecord` hoặc `CashTransaction` internals vào response/resultJson vì các bảng ledger/document đã là source of truth. POS checkout validate ca bằng `getShift(shiftId)` rồi kiểm tra status/scope/cashier, không scan open shift theo cashier. Regression `google-workspace-adapter`, `command-sheet-repository`, `command-coordinator`, `finance-sheet-repository`, `sales-service`, `sales-composition` và acceptance flow cover các rule này. | Tối ưu này giảm I/O phụ, payload command journal, durable command preflight, duplicate balance lookup và shift lookup cho write path chung mà không đổi nghiệp vụ/domain source of truth. Cần `npm run verify`, deploy `/dev` và benchmark POS UI lại trước khi cập nhật trạng thái P0. |
| 2026-08-02 | Finance master aging local pass | `CashDrawer`/`PaymentMethod` contract thêm `directSaleEnabled`; `ObligationDTO` thêm `dueDate`; `finance.master.get`, `finance.cashDrawer.upsert`, `finance.paymentMethod.upsert`, `finance.aging.get` được đăng ký trong shared operations, schema, API composition và local fake backend. `createFinanceService` tính aging projection từ Receivable/Payable ledger theo `dueDate`, Branch, loại công nợ và trạng thái; mặc định loại Settled/Reversed. | `tests/apps-script/finance/finance-service-master-aging.test.ts`, `tests/apps-script/finance/finance-composition.test.ts`, `tests/shared/finance-contracts.test.ts`, `tests/web/local-fake-backend.test.ts` pass; backend slice không cho sửa số dư trực tiếp và không lưu secret payment config. UI Finance và performance/concurrency matrix vẫn triển khai ở slice sau. |
| 2026-08-01 | Apps Script POS helper benchmark sau command fast path và single-line stock optimization | `npm run verify` pass 109 files/362 tests, `npm run deploy:test` push `/dev`. Apps Script editor chạy `runPosAcceptanceDrill` 3 mẫu sau deploy: checkoutMs `[1596, 2503, 1639]`, `command.totalWithLockMs` `[1596, 2503, 1639]`, `command.findExistingMs` không còn, `command.findCachedExistingMs` `[28, 22, 22]`, `stockCheckMs=0`, appendMs `[685, 2448, 581]`, flushMs `[1472, 812, 704]`. Functional checkout đều `ok=true`, `Completed/Paid`. | Server-side POS command helper đã đạt mục tiêu p95 ≤ 3s trong 3 mẫu sau tối ưu. P0 `pos-acceptance-benchmark` vẫn cần UI/browser benchmark mới để xác nhận client end-to-end, vì helper còn bao gồm seed/projection overhead và Chrome connector hiện không reset được POS UI ổn định từ trạng thái success. |

## 5. Decision required

1. Bản bán đầu có bắt buộc hỗ trợ lot/serial, chuyển kho và kiểm kê đầy đủ không, hay khóa UI/operation cho tenant nhỏ cho tới phase sau?
2. Production adapter nên cutover toàn bộ domain ngay hay theo lát cắt sellable trước: Platform/Auth/Config + Catalog/CRM + Inventory/Finance/Sales/POS + Reporting/Operations?
3. Restore drill sẽ chạy trên Apps Script test project nào; `.clasp.json` và `scriptId` phải nằm ngoài Git.

## 6. Gate rule

Release status chỉ được chuyển từ `Blocked` sang `Ready` khi:

- `npm run release:readiness` không còn P0 gap.
- `npm run verify` pass.
- POS benchmark có evidence.
- Deployment/migration/restore drill có evidence.
- Mọi P1 gap được phân loại trong `docs/architecture/release-scope-baseline.md` và được user duyệt.
